package LinkedLists;
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode(int x) { val = x; }
 * }
 */
public class DeleteNodeIfValisSpecifiedWithoutHead {
    node.val = node.next.val;
    // Delete the next node by bypassing it
    node.next = node.next.next;
}
}
